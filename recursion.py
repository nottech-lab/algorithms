import math
import os
import random
import re
import sys

# Recursion challenge on Algorithm
def powerSum(X, N):

    if __name__ == '__main__':
        fptr = open(os.environ['OUTPUT_PATH'], 'w')

        X = int(raw_input())

        N = int(raw_input())

        result = powerSum(X, N)

        fptr.write(str(result) + '\n')

        fptr.close()